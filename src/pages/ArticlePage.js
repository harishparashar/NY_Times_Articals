import React from "react";
import ArticleDetails from "../components/ArticleDetails";

const ArticlePage = ({ articles }) => {
  return <ArticleDetails articles={articles} />;
};

export default ArticlePage;
